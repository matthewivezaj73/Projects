serviceAccount:
  create: false

app:
  name: retail-backend

replicaCount: 3

image:
  repository: retail-backend
  tag: "1.0"
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 5000

ingress:
  enabled: false

httpRoute:
  enabled: false

autoscaling:
  enabled: false
