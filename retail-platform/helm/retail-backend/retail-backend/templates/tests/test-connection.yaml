apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "retail-backend.fullname" . }}-test-connection"
  labels:
    {{- include "retail-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "retail-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
