apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-platform.fullname" . }}
  labels:
    {{- include "retail-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retail-platform.selectorLabels" . | nindent 4 }}
